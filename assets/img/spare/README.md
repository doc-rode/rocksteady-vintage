# Spare imagery

Unused-but-kept images. Nothing in this folder is referenced by the live pages — it's a
holding area for photos/art we may swap back in later. Optimize new photos to web-sized JPEG
(~1600px wide, ~200–260 KB) before wiring them into a page.

- (empty for now — re-drop `photo-ridge-run` here if you still have the original)
